[build]
  command = "npm run build"
  publish = "out"

# La vista previa se bloquea con NEXT_PUBLIC_NOINDEX=1 (meta robots + robots.txt),
# que funciona en cualquier hosting. El bloque [[headers]] anterior sólo servía
# en Netlify y daba una falsa sensación de protección en GitHub Pages.
